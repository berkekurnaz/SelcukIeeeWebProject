@model IEnumerable<Etkinliks>
@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_LayoutAdmin.cshtml";
}

<script src="https://webthemez.com/demo/brilliant-free-bootstrap-admin-template/assets/js/jquery-1.10.2.js"></script>
<script>
    $(document).ready(function () {
        $('#myTable').dataTable();
    });
</script>

<div class="main-content">
    <div class="container-fluid">

        @if (Model.Count() > 0)
        {
            <div class="row">
                <div class="col-md-12">
                    <div class="card">
                        <div class="card-block">
                            <h4 class="card-title">Etkinlikler Listesi</h4>
                            <div class="table-overflow table-responsive">
                                <table id="myTable" class="table table-bordered">
                                    <thead>
                                        <tr>
                                            <th>Etkinlik Adı</th>
                                            <th>Etkinlik Tarihi</th>
                                            <th>İşlemler</th>
                                        </tr>
                                    </thead>
                                    <tbody>
                                        @foreach (var item in Model)
                                        {
                                            <tr>
                                                @if (item.EtkinlikAd.Length > 30)
                                                {
                                                    <td>@item.EtkinlikAd.Substring(0, 30)</td>
                                                }
                                                else
                                                {
                                                    <td>@item.EtkinlikAd</td>
                                                }
                                                <td>@item.Tarih</td>
                                                <td>
                                                    <a href="/YonetimFotograflar/Fotograflar/@item.Id" class="btn btn-success btn-sm"><i class="fa fa-pencil" aria-hidden="true"></i> Fotoğrafları Gör</a>
                                                </td>
                                            </tr>
                                        }
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        }
        else
        {
            <div class="card">
                <div class="card-block">
                    <h4 class="card-title">Uyarı Kutusu</h4>
                    <div class="mrg-top-20">
                        <p>Burada Herhangi Bir İçerik Bulunmamaktadır...</p>
                    </div>
                </div>
            </div>
        }


    </div>
</div>

